{
  "name": "expo-ecommerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin",
    "build:admin": "npm run build --prefix admin",
    "build": "npm run install:all && npm run build:admin",
    "start": "npm run start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mariomarquesto/expo-ecommerce.git"
  },
  "license": "ISC"
}
